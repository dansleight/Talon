@model List<Test.Mvc.Models.PersonModel>
@{
    ViewBag.Title = "Scrolling";
}

<div class="panel panel-primary">
    <div class="panel-heading">
        <h3 class="panel-title">Scrolling and Frozen Columns</h3>
    </div>
    <div class="panel-body panel-grid">
        @(Html.Talon().Grid(Model).Name("people")
            .Columns(c =>
            {
                c.Bound(m => m.ID);
                c.Bound(m => m.Name).Width(300);
                c.Bound(m => m.Country).Width(400);
                c.Bound(m => m.Phone).Width(250);
                c.Bound(m => m.Email).Width(300);
            })
            .PageSize(25)
            .ScrollX()
            .ScrollY(400)
            .Freeze()
        )
    </div>
</div>

<div class="col-md-6">
    <div class="panel panel-default">
        <div class="panel-heading">
            <h3 class="panel-title">Markup</h3>
        </div>
        <div class="panel-body">
<pre style="font-family:Consolas;font-size:13;color:black;background:white;"><span style="background:yellow;">@@(</span>Html.Talon().Grid(Model).Name(<span style="color:#a31515;">"people"</span>)
&nbsp;&nbsp;&nbsp;&nbsp;.Columns(c&nbsp;=&gt;
&nbsp;&nbsp;&nbsp;&nbsp;{
&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;c.Bound(m&nbsp;=&gt;&nbsp;m.ID);
&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;c.Bound(m&nbsp;=&gt;&nbsp;m.Name).Width(300);
&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;c.Bound(m&nbsp;=&gt;&nbsp;m.Country).Width(400);
&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;c.Bound(m&nbsp;=&gt;&nbsp;m.Phone).Width(250);
&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;c.Bound(m&nbsp;=&gt;&nbsp;m.Email).Width(300);
&nbsp;&nbsp;&nbsp;&nbsp;})
&nbsp;&nbsp;&nbsp;&nbsp;.PageSize(25)
&nbsp;&nbsp;&nbsp;&nbsp;.ScrollX()
&nbsp;&nbsp;&nbsp;&nbsp;.ScrollY(400)
&nbsp;&nbsp;&nbsp;&nbsp;.Freeze()
<span style="background:yellow;">)</span></pre>
        </div>
    </div>
</div>
<div class="col-md-6">
    <div class="panel panel-info">
        <div class="panel-heading">
            <h3 class="panel-title">Notes</h3>
        </div>
        <div class="panel-body">
            <p>
                <strong>ScrollX() arguments:</strong>
                <ul>
                    <li>.ScrollX() sets X-Scrolling to true</li>
                    <li>.ScrollX(bool scrollX) sets X-Scrolling to provided value</li>
                </ul>
                <em style="color: silver">(not sure why you would ever need to set it to false, as that is the default)</em>
            </p>
            <p>
                <strong>ScrollY() arguments:</strong>
                <ul>
                    <li>.ScrollY() sets the height to 300px</li>
                    <li>.ScrollY(int scrollY) sets the height to {scrollY}px</li>
                    <li>.ScrollY(string scrollY) sets the height to {scrollY}</li>
                </ul>
            </p>
            <p>
                <strong>Freeze() arguments</strong>
                <ul>
                    <li>.Freeze() will freeze the first column</li>
                    <li>.Freeze(int freeze) will freeze {freeze} column(s)</li>
                </ul>
            </p>
        </div>
    </div>
</div>



